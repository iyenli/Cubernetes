package options

const (
	BUILDDIR     = "./build/"
	CUBEROOT     = BUILDDIR + "cuberoot"
	APISERVER    = BUILDDIR + "apiserver"
	CUBELET      = BUILDDIR + "cubelet"
	CUBECTL      = BUILDDIR + "cubectl"
	LOGDIR       = "/var/log/cubernetes"
	APISERVERLOG = LOGDIR + "apiserver.log"
	CUBELETLOG   = LOGDIR + "cubelet.log"
)
