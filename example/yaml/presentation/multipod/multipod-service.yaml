apiVersion: v1
kind: Service
metadata:
  name: multipod-svc
spec:
  selector:
    svcname: proxy
  ports:
    - name: nginx-port
      protocol: TCP
      port: 8080
      targetPort: nginx-port
    - name: tomcat-port
      protocol: TCP
      port: 8080
      targetPort: tomcat-port